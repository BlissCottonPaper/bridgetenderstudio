import { Hero } from '@/components/Hero';

// Phase 1 · Milestone 1 — the foundation and the hero.
// Featured Projects, Featured Platform (Lantern), What We Build, Process, and
// the closing CTA land in the next milestone, once the feeling is approved.
export default function HomePage() {
  return (
    <>
      <Hero />
    </>
  );
}
